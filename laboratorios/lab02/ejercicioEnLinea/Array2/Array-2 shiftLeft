public int[] shiftLeft(int[] nums) {
  if(nums.length == 0)
  {
    return new int[0];
  }
  int[] sol = new int[nums.length];
  for(int i = 1; i < nums.length; i++)
  {
    sol[i-1]= nums[i];
  }
  sol[sol.length - 1] = nums[0];
  return sol;
}
